version: '3.8'

services:
  rss-app:
    build: .
    ports:
      - "8001:8001"
    environment:
      - PYTHONPATH=/app
    volumes:
      - ./src:/app/src
      - ./.env:/app/.env
    restart: unless-stopped 